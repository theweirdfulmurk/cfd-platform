# Математический бриф для написания НИРС

Состояние от 2026-06-04. Вход для Claude-писателя НИРС (см. структуру в
[NIRS_PLAN.md](NIRS_PLAN.md)). Формальная модель, псевдокод алгоритмов **по
фактическому коду** (`scheduler/algorithms/*.go`), методология и источники.

## Инструкция писателю
1. НИРС = теория и методы. **Никаких численных результатов/Table/графиков/
   p-value конкретных прогонов** (это ВКР). Гипотезу и методологию — описывать
   как формулировку/метод, без эмпирической проверки.
2. **Не описывать инженерию/деплой** (backend, образы, CI, cluster-up) — это
   отчёт по практике. Здесь — формализм, алгоритмы, методология.
3. Псевдокод сверяй с кодом (`scheduler/algorithms/`); не выдумывай шаги.
4. Стиль как у Бердышева: нумерованные формулы, листинги псевдокода с подписью.
   Русский, академический. ~22–26 стр.
5. **Имена людей — кириллицей** в тексте (Мюллер-Мербах, Купманс, Бекман, Буркард,
   Хофлер, Се (Xie)…); список источников — в оригинале (латиница). Аббревиатуры с
   русским стандартом русифицировать (FEM→МКЭ, CFD→ВГД); международные без
   стандарта — латиницей с расшифровкой (QAP, MPI, HNSW, MUMPS), не выдумывать
   «КЗН». Названия технологий и имена процедур в псевдокоде — латиницей.

---

# Часть A. Формальная модель размещения (глава 1)

## A.1 Обозначения
- Ранги (MPI-процессы) V = {1, …, N}; узлы кластера U = {1, …, M}, причём M ≥ N
  (узлов больше, чем рангов, — «пространство выбора» для планировщика).
- **Граф коммуникации** F — симметричная матрица N×N, F(i,j) ≥ 0, F(i,i) = 0;
  F(i,j) = w_ij — интенсивность обмена между рангами i и j (число общих граней/
  узлов/dofs, см. часть C).
- **Матрица латентности** L — симметричная M×M, L(a,b) ≥ 0, L(a,a) = 0; L(a,b) —
  попарная задержка между узлами a и b.
- **Размещение** π: {1,…,N} → {1,…,M} — инъекция (каждый ранг на свой узел,
  anti-affinity «один ранг на узел»). Placement[i] = π(i).

## A.2 Целевая функция (точно как `algorithms.Cost`)
Стоимость размещения — суммарная «латентность, взвешенная коммуникацией»:

    C(π) = Σ_{1 ≤ i < j ≤ N} F(i,j) · L(π(i), π(j)).            (1)

(Сумма по неупорядоченным парам i<j — верхний треугольник; F, L симметричны,
поэтому (1) = ½ Σ_{i≠j} F(i,j)·L(π(i),π(j)).) Задача:

    π* = arg min_π C(π).                                         (2)

## A.3 Связь с QAP и сложность
(2) — частный случай **квадратичной задачи о назначениях** в форме
Купманса–Бекмана:

    min_π Σ_{i,j} f_{ij} · d_{π(i)π(j)},                         (3)

где «потоки» f = F, «расстояния» d = L; отличие от классической QAP — N ≤ M
(инъекция, а не перестановка). QAP **NP-трудна**; более того (Sahni, Gonzalez,
1976) для неё не существует полиномиального ε-приближения при P ≠ NP. Точное
решение (ветвей и границ) практически неприменимо при росте N → обоснование
эвристик (глава 2).

## A.4 Плотность графа задачи
Структурная плотность F (характеристика расчёта, ось сравнения в ВКР):

    ρ = |{ (i,j) : i<j, F(i,j) > 0 }| / ( N(N−1)/2 ).            (4)

Качественно: sparse FVM (ρ мала), implicit FEM с MUMPS (ρ велика). Используется
как теоретическая характеристика задачи (числа — в ВКР).

---

# Часть B. Алгоритмы размещения (глава 2 — ядро)

Все три реализованы в `scheduler/algorithms/`. Ниже — точный псевдокод.

## B.1 Случайное размещение (baseline) — `random.go`
Имитирует «отсутствие topology-awareness». Воспроизводимость: seed = FNV-1a(jobID),
для ранга r используется поток с зерном (seed + r).

```
RANDOM-PLACE(F, L, M, seed):
  p ← [-1,…,-1]                       # размещение, длина N
  occupied ← ∅
  for r ← 0 … N-1:
    free ← { a ∈ {0..M-1} : a ∉ occupied }
    rng ← PRNG(seed + r)              # детерминированно по jobID и рангу
    a ← uniform-choice(free, rng)
    p[r] ← a; occupied ← occupied ∪ {a}
  return p
```

## B.2 Жадный онлайн-алгоритм — `greedy.go`
Каждый ранг ставится на свободный узел, минимизирующий частичную стоимость связи
с **уже размещёнными** рангами. Частичная стоимость кандидата a для ранга r:

    g(r, a) = Σ_{r′ : p[r′] ≥ 0} F(r, r′) · L(a, p[r′]).         (5)

```
GREEDY-NODE(F, L, p, r, candidates):     # online: вызывается на приход пода r
  used ← { p[r′] : p[r′] ≥ 0 }
  best ← ⊥; bestCost ← +∞
  for a ∈ (candidates \ used):
    c ← Σ_{r′ : p[r′] ≥ 0, r′ ≠ r} F[r][r′] · L[a][p[r′]]      # (5)
    if c < bestCost: bestCost ← c; best ← a
  return best
```
Сложность одного вызова O(|placed|·M); за задачу — O(N²) (при M ~ O(N)).
Онлайн-природа → отказоустойчивость (под приходит независимо), но решение
субоптимально (не видит будущих рангов).

## B.3 Эвристика Müller-Merbach (офлайн) — `mueller_merbach.go`
Конструктивная эвристика QAP с каскадными переназначениями (Burkard,
Dell'Amico, Martello, «Assignment Problems», SIAM 2009, §8.2.1). Ранги
обрабатываются в порядке убывания суммы строки F (тяжёлые коммуникаторы
первыми); для каждого ранга оценивается полная Δ-стоимость каждого узла, включая
стоимость вытеснения текущего занимающего.

Порядок:

    order = ранги, отсортированные по убыванию  s(i) = Σ_j F(i,j).   (6)

Δ свободного узла a для ранга r (по уже размещённым):

    δ_free(r, a) = Σ_{o : p[o] ≥ 0, o ≠ r} F(r, o) · L(a, p[o]).      (7)

Полная стоимость кандидата a (если a занят рангом d — с учётом переселения d):

    candCost(r, a) =
        δ_free(r, a),                                   если a свободен;
        δ_new(r, a, d) + min_{b свободен, b≠a} [ δ_disp(d, b) + F(r,d)·L(a,b) ],  иначе,   (8)

    где δ_new(r,a,d) = Σ_{o≠r,d : p[o]≥0} F(r,o)·L(a,p[o]),
          δ_disp(d,b) = Σ_{o≠r,d : p[o]≥0} F(d,o)·L(b,p[o]).

```
MUELLER-MERBACH(F, L, M):
  p ← [-1,…,-1]; used ← ∅; nodeToRank ← [-1,…,-1]
  order ← argsort_desc( s(i)=Σ_j F[i][j] )           # (6), ties: по индексу
  for r ∈ order:
    best ← ⊥; bestCost ← +∞
    for a ← 0 … M-1:
      c ← candCost(r, a)                              # (8)
      if c < bestCost: bestCost ← c; best ← a
    if used[best]:                                    # каскад: вытеснить занимающего
      d ← nodeToRank[best]
      p[r] ← best; nodeToRank[best] ← r; p[d] ← -1
      b* ← arg min_{b свободен, b≠best} δ_free(d, b)  # (7)
      p[d] ← b*; used[b*] ← true; nodeToRank[b*] ← d
    else:
      p[r] ← best; used[best] ← true; nodeToRank[best] ← r
  return p
```
Сложность O(N²·M) на задачу (≤ ~100 мс для N=128, M=100). Офлайн: полное
размещение строится один раз и кэшируется на jobID; каждый под получает свой
целевой узел из готового π. Лучшее качество на плотных F (учитывает все ранги +
один уровень look-ahead на вытеснение), но чувствительно к отказу узла (требует
пересчёта).

## B.4 Сопоставление (теоретически, без чисел)
| Свойство | random | greedy | Müller-Merbach |
|---|---|---|---|
| Режим | онлайн | онлайн | офлайн |
| Сложность | O(N·M) | O(N²) | O(N²·M) |
| Учёт будущих рангов | нет | нет | да |
| Каскадные переназначения | нет | нет | да (1 уровень) |
| Ожидаемое качество с ростом ρ | базовое | среднее | выше |

→ листинги-кандидаты: (1) и `Cost`; псевдокод MUELLER-MERBACH; псевдокод GREEDY-NODE.

---

# Часть C. Извлечение графа коммуникации (глава 3)

Веса w(i,j) = F(i,j) извлекаются из артефактов **доменной декомпозиции** до
запуска расчёта (без инструментирования MPI). Три класса:

- **OpenFOAM (sparse FVM)**: `decomposePar` → `processor*/constant/polyMesh/
  boundary`; в блоке `processorN` поле nFaces = число общих граней между
  субдоменами → w(i,j) = nFaces(i,j).
- **OpenRadioss (explicit FEM)**: sed-патч `IDB_METIS=1` → дамп графа в формате
  METIS (`input.graph0`); `gpmetis input.graph0 N` → разбиение; вес ребра =
  число смежностей между субдоменами.
- **Code_Aster (implicit FEM + MUMPS)**: `medpartitioner --create-boundary-faces`
  → joints в MED-файлах; MEDLoader: getJoints → getCorrespondence; w(i,j) =
  число общих узлов между субдоменами i и j.

Результат — симметричная разрежённая F (тип `decomp.Graph`: рёбра {I<J, Weight},
`Matrix()` → плотная N×N с нулём на диагонали). Формализуй единый pipeline и
свойства F (симметрия, разрежённость, связь ρ с классом решателя). Подробности —
[EXTRACTION.md](EXTRACTION.md).

---

# Часть D. Методология оценки (глава 4 — метод, НЕ результаты)

## D.1 Измеряемая величина
Суммарное MPI-время T из профиля **mpiP** (LD_PRELOAD): агрегат по всем рангам
секции «MPI Time» отчёта. Связь с C(π): размещение влияет на L(π(i),π(j)) →
на время коллективов/обменов → на T. (Формально T — целевая эмпирическая
метрика; C(π) — её аналитический прокси.)

## D.2 Эмуляция multi-AZ
Контраст латентности задаётся `tc qdisc netem` (Linux Traffic Control): логические
зоны, контролируемые межзонные RTT. L(a,b) формируется из зональной
принадлежности узлов. Обоснование — воспроизводимость, независимость от
облачного провайдера (стандартная академическая методология).

## D.3 Обоснование N и гранулярности
Число рангов N и размер задачи per rank выбираются по литературным порогам
эффективной гранулярности (cells/elements/dofs на ядро) — чтобы измеряемая
разница отражала эффект размещения, а не доминирующий MPI-overhead.
Представить как **проектное обоснование методики** (пороги — из литературы).

## D.4 Статистический аппарат (как метод; формулы)
Для каждой ячейки (решатель, алгоритм):
1. Нормальность — критерий **Шапиро–Уилка** (статистика W, уровень α=0.05).
2. Доверительный интервал среднего:
   - нормально → Стьюдент: x̄ ± t_{1−α/2, n−1} · s/√n;
   - иначе → перцентильный **bootstrap** (B ресэмплов, 2.5/97.5 перцентили).
3. Парные сравнения алгоритмов: **парный t-критерий** / **критерий Уилкоксона**
   (знаково-ранговый); при направленной гипотезе — односторонние.

Гипотеза (как формулировка, без проверки): выигрыш Müller-Merbach над random
монотонно растёт с плотностью ρ и числом рангов N. Угрозы валидности — перечислить.

---

# Часть E. Источники по главам (выверенные, реальные)

**Глава 1 (модель/QAP/мотивация):**
- T. Koopmans, M. Beckmann. *Assignment Problems and the Location of Economic Activities.* Econometrica, 1957 — постановка QAP.
- S. Sahni, T. Gonzalez. *P-complete approximation problems.* J. ACM, 1976 — NP-трудность QAP и неаппроксимируемость.
- T. Hoefler, T. Schneider, A. Lumsdaine. *Characterizing the Influence of System Noise on Large-Scale Parallel Applications.* SC'10 — амплификация синхронизации.
- MPI Standard. https://www.mpi-forum.org/docs/ — модель коллективов/обменов.

**Глава 2 (алгоритмы):**
- R. Burkard, M. Dell'Amico, S. Martello. *Assignment Problems.* SIAM, 2009 — §8.2.1, эвристика Müller-Merbach (источник, на который ссылается код).
- E. Çela. *The Quadratic Assignment Problem: Theory and Algorithms.* Kluwer, 1998 — обзор эвристик (опционально).

**Глава 3 (извлечение F):**
- H. Weller et al. *A tensorial approach to CFD using object-oriented techniques.* Computers in Physics, 1998 (OpenFOAM); + decomposePar (User Guide).
- G. Karypis, V. Kumar. *A Fast and High Quality Multilevel Scheme for Partitioning Irregular Graphs.* SIAM J. Sci. Comput., 1998 (METIS).
- OpenRadioss — https://www.openradioss.org/ ; Code_Aster / Salome MEDCoupling — https://www.code-aster.org/, https://docs.salome-platform.org/.

**Глава 4 (методология/оценка):**
- J. Vetter, C. Chambreau. *mpiP: Lightweight, Scalable MPI Profiling.* — метрика MPI-time.
- Linux `tc-netem` — https://man7.org/linux/man-pages/man8/tc-netem.8.html — эмуляция латентности.
- S. Shapiro, M. Wilk. *An analysis of variance test for normality.* Biometrika, 1965.
- B. Efron. *Bootstrap Methods: Another Look at the Jackknife.* Annals of Statistics, 1979.
- F. Wilcoxon. *Individual Comparisons by Ranking Methods.* Biometrics Bulletin, 1945.
- (t-критерий) Student. *The probable error of a mean.* Biometrika, 1908; B. Welch, 1947.
- Xie 2026 — методологическая опора (Burstable QoS; рост эффекта с N). Полные данные — [RELATED_WORK.md](RELATED_WORK.md).
- (контекст HPC-планировщиков K8s) Volcano (CNCF); Fluence/Flux (LLNL) — кратко, как смежные подходы.

В список вносить только реально процитированные. Не выдумывать ссылки.

---

# Часть F. Листинги (выбрать 2–3)
| Кандидат | Источник | Глава |
|---|---|---|
| Целевая функция C(π) — формула (1) + `Cost` | `algorithms/types.go` | 1/2 |
| Псевдокод MUELLER-MERBACH | `algorithms/mueller_merbach.go` | 2 |
| Псевдокод GREEDY-NODE | `algorithms/greedy.go` | 2 |

Один псевдокод-листинг на ключевой алгоритм, с подписью «Листинг N – …».
